FROM golang:1.23-alpine AS builder

ARG VERSION=dev

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN go build -ldflags "-X github.com/shrimpsizemoose/kanelbulle/internal/bot.Version=${VERSION}" -o /server ./cmd/server

FROM alpine:3.19
LABEL org.opencontainers.image.source=https://github.com/shrimpsizemoose/kanelbulle
COPY --from=builder /server /server
ENTRYPOINT ["/server"]
